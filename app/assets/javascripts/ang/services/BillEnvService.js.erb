app.factory('BillEnv', function () {
return {
    getMySecretEnv: function () {
        if("<%= ENV['LEGISCAN_API_KEY'] %>" !== 'null'){
          return "<%= ENV['LEGISCAN_API_KEY'] %>";
          //  ###  html_safe
      }
      console.log('legiscan api not passing')
    }
  }
});
